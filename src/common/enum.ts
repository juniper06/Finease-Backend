export enum Role {
    CEO = "CEO",
    CFO = "CFO",
    ADMIN = "ADMIN",
    GUEST = "GUEST",
}

export enum RequestStatus {
    PENDING = "PENDING",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED"
}